#!/bin/sh
set -eu
SCRIPT_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
PLUGIN_ROOT=$(CDPATH= cd -- "$SCRIPT_DIR/.." && pwd)
PYTHONPATH="$PLUGIN_ROOT/runtime${PYTHONPATH:+:$PYTHONPATH}" exec python3 -m hb_eval_review.cli "$@"
